const SectionTitle = ({ text }) => {
  return (
    <div className=" m-5 text-black text-center">
      <h2 className="text-3xl font-thin text-darkMain uppercase">{text}</h2>
    </div>
  );
};
export default SectionTitle;
