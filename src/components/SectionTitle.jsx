const SectionTitle = ({ text }) => {
  return (
    <div className=" m-5 text-black">
      <h2 className="text-4xl font-small text-darkMain uppercase">{text}</h2>
    </div>
  );
};
export default SectionTitle;
